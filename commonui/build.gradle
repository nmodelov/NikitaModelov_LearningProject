apply plugin: 'com.android.library'
apply from : '../android.module.gradle'

dependencies {
    implementation dep('ru.tinkoff.decoro:decoro')
    implementation dep('com.google.android.material:material')
}
